Для вашего репозитория **Ippon-UPS-SNMP** на GitHub отличный README должен не только объяснять, что это такое, но и помогать пользователям с установкой и настройкой.

Вот готовый текст для файла `README.md`. Скопируйте содержимое ниже:

---

# Ippon UPS SNMP Integration for Home Assistant

Интеграция для мониторинга источников бесперебойного питания (ИБП) **Ippon** (и других совместимых моделей, использующих протокол SNMP v3).

Данная интеграция позволяет получать данные о состоянии электросети, уровне заряда батареи, нагрузке и других параметрах напрямую в Home Assistant через локальную сеть.

## ✨ Особенности

* **SNMP v3**: Поддержка современного и безопасного протокола авторизации (MD5/SHA).
* **13 сенсоров**: Полный мониторинг состояния (напряжение, частота, температура, остаточное время работы и др.).
* **Легкая настройка**: Настройка через графический интерфейс (Config Flow) без необходимости править YAML.
* **Поддержка Python 3.13**: Оптимизировано для последних версий Home Assistant.

## 📊 Список доступных сенсоров

Интеграция автоматически создает следующие объекты:

* **Статус батареи** (Battery Status)
* **Заряд батареи** (в процентах)
* **Напряжение батареи**
* **Время автономной работы** (в минутах)
* **Напряжение на входе и выходе**
* **Частота тока** (входная/выходная)
* **Температура системы**
* **Статус системы** (Line, Battery, Bypass и т.д.)
* **Результат последнего теста батареи**

## 🚀 Установка

### Способ 1: Вручную

1. Скачайте репозиторий.
2. Скопируйте папку `ippon_ups_snmp` из `custom_components` в папку `config/custom_components/` вашего Home Assistant.
3. Перезапустите Home Assistant.

### Способ 2: HACS (в будущем)

1. Откройте HACS.
2. Нажмите три точки в правом верхнем углу и выберите **Custom repositories**.
3. Добавьте ссылку `https://github.com/Eugen417/Ippon-UPS-SNMP` и выберите категорию **Integration**.
4. Нажмите «Установить».

## ⚙️ Настройка

1. Перейдите в **Настройки** -> **Устройства и службы**.
2. Нажмите **Добавить интеграцию** и введите в поиске `Ippon UPS SNMP`.
3. Заполните данные:
* **Host**: IP-адрес вашей SNMP-карты в ИБП.
* **Port**: Обычно `161`.
* **Username**: Логин SNMP v3.
* **Auth Key**: Пароль авторизации.
* **Protocol**: Выберите протокол (например, `hmac-md5`).



## 📝 Пример автоматизации

Вы можете настроить уведомление в Telegram при переходе на батарею:

```yaml
alias: "Уведомление: ИБП перешел на батарею"
trigger:
  - platform: state
    entity_id: sensor.ups_192_168_1_27_output_source
    to: "battery"
action:
  - service: notify.telegram
    data:
      message: "Внимание! Отключили электричество. ИБП работает от батареи."

```

## ⚠️ Требования

* Установленная библиотека `pysnmp-lextudio`.
* SNMP-карта в ИБП с поддержкой v3.

---

### Как добавить это на GitHub:

1. В корне вашего репозитория нажмите **Add file** -> **Create new file**.
2. Назовите файл `README.md`.
3. Вставьте текст выше.
4. Нажмите **Commit changes**.

Ваш проект теперь выглядит профессионально и готов к использованию другими людьми!
